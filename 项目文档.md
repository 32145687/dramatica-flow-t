# Dramatica Flow - AI 小说创作助手

## 项目概览

**Dramatica Flow** 是一款基于 Android 的 AI 驱动小说创作应用，采用 Kotlin + Jetpack Compose 构建，集成 DeepSeek 等大语言模型 API，提供从灵感构思到完稿审校的完整创作流程。

- **平台**: Android (minSdk 26, targetSdk 35)
- **语言**: Kotlin
- **UI 框架**: Jetpack Compose + Material3
- **版本**: 1.1.0 (versionCode 3)
- **架构模式**: MVVM (ViewModel + Repository + Room)
- **包名**: `com.dramatica.flow`

---

## 一、核心架构

### 1.1 整体架构

```
┌─────────────────────────────────────────────┐
│                  UI 层                       │
│  DramaticaFlowApp  →  Screens  →  Components│
├─────────────────────────────────────────────┤
│               ViewModel 层                   │
│  MainViewModel  ←→  BookSession              │
├─────────────────────────────────────────────┤
│               Repository 层                  │
│  LocalRepository (Room DB)                   │
│  AiRepository   (API 通信)                   │
├─────────────────────────────────────────────┤
│               Agent 层                       │
│  ArchitectAgent · ContentRectifier           │
│  PostWriteValidator                          │
└─────────────────────────────────────────────┘
```

### 1.2 技术栈

| 层级 | 技术 |
|------|------|
| UI | Jetpack Compose + Material3 + Material Icons Extended |
| 数据库 | Room (本地 SQLite) |
| 配置存储 | SharedPreferences + DataStore |
| 异步 | Kotlin Coroutines + StateFlow/SharedFlow |
| JSON | kotlinx-serialization-json |
| AI 通信 | HttpURLConnection (兼容 OpenAI API 格式) |

---

## 二、项目目录结构

```
dramatica-local/
├── app/
│   ├── build.gradle.kts          # 构建配置
│   └── src/main/
│       ├── java/com/dramatica/flow/
│       │   ├── MainActivity.kt           # 应用入口
│       │   ├── data/
│       │   │   ├── AppDatabase.kt        # Room 数据库 (13张表, 13个DAO)
│       │   │   ├── LocalRepository.kt    # 本地数据仓库
│       │   │   ├── AiRepository.kt       # AI API 通信
│       │   │   ├── ArchitectAgent.kt     # 建筑师 Agent (章节规划)
│       │   │   ├── ContentRectifier.kt   # 内容整改管线
│       │   │   └── PostWriteValidator.kt # 写后质量验证
│       │   └── ui/
│       │       ├── DramaticaFlowApp.kt   # 主应用 Composable
│       │       ├── viewmodel/
│       │       │   ├── MainViewModel.kt  # 核心 ViewModel
│       │       │   └── BookSession.kt    # 书籍会话管理
│       │       ├── screens/
│       │       │   ├── HomeScreen.kt         # 书架首页
│       │       │   ├── FlowScreen.kt         # 7步创作流程
│       │       │   ├── FlowStepDetailScreen.kt # 步骤详情
│       │       │   ├── ReferenceFlowScreen.kt  # 参考创作工作台
│       │       │   ├── NovelAnalysisScreen.kt  # 参考小说分析
│       │       │   ├── WritingScreen.kt        # 写作编辑器
│       │       │   ├── WritingSkillScreen.kt   # 写作技能蒸馏
│       │       │   ├── CharactersScreen.kt     # 角色档案
│       │       │   ├── TrackingScreen.kt       # 一致性追踪
│       │       │   ├── TimelineScreen.kt       # 时间线
│       │       │   ├── TxtSplitScreen.kt       # TXT文件拆分
│       │       │   └── SettingsScreen.kt       # API设置
│       │       ├── components/
│       │       │   ├── Components.kt       # 通用UI组件库
│       │       │   └── AppSidebar.kt       # 侧边栏导航
│       │       └── theme/
│       │           ├── Color.kt            # 配色方案
│       │           └── Theme.kt            # 主题与排版
│       └── res/                            # 资源文件
```

---

## 三、数据库设计

### 3.1 实体表 (13张)

| 表名 | 实体类 | 用途 |
|------|--------|------|
| `books` | `BookEntity` | 书籍元数据（书名、题材、进度） |
| `chapters` | `ChapterEntity` | 章节内容（草稿/定稿） |
| `characters` | `CharacterEntity` | 角色档案（7种Dramatica原型） |
| `hooks` | `HookEntity` | 伏笔追踪（open/resolved/warning） |
| `causal_links` | `CausalLinkEntity` | 因果链（因→事→果→决） |
| `relationships` | `RelationshipEntity` | 角色关系网络（强度 -100~100） |
| `emotions` | `EmotionEntity` | 角色情感快照（强度 1-10） |
| `timeline_events` | `TimelineEntity` | 时间线事件（冲突/揭示/情感/伏笔） |
| `dramatica_projects` | `DramaticaProjectEntity` | 创作项目完整状态 |
| `known_info` | `KnownInfoEntity` | 信息边界（角色所知信息） |
| `narrative_threads` | `NarrativeThreadEntity` | 多线叙事管理 |
| `chapter_beats` | `ChapterBeatEntity` | 戏剧节拍（10种节拍类型） |
| `writing_skills` | `WritingSkillEntity` | 写作技能蒸馏结果 |

### 3.2 关键数据模型

**StoryConfig** — 故事配置（核心数据结构）:
- `title` / `genre` / `briefIdea` — 基础信息
- `coreSetting` / `characters` / `outline` — AI生成内容
- `colloquialStyle` / `useMemes` — 写作风格开关
- `referenceAnalysis` — 参考小说分析结果

**DramaticaStep** — 7步创作流程枚举:
1. `BASIC_INFO` — 基础信息
2. `WORLD_BUILDING` — 世界观构建
3. `CHARACTER_DESIGN` — 角色设计
4. `OUTLINE` — 大纲规划
5. `WRITING` — 章节创作
6. `AI_RESULT` — AI创作结果
7. `TIMELINE` — 完稿审校

---

## 四、核心功能

### 4.1 双重创作模式

#### 模式一：全新创作（7步向导流程）
从零开始，AI 辅助完成完整的创作流程：

1. **基础信息** — 填写书名、题材、核心创意、目标章节数
2. **世界观构建** — AI 自动生成世界观设定（世界背景、规则体系、关键地点、历史）
3. **角色设计** — 基于 Dramatica 叙事理论，AI 设计 7 种角色原型（主角、反派、冲击者、守护者、阻碍者、伙伴、怀疑者）
4. **大纲规划** — AI 生成 5 卷结构分卷大纲，自动分配章节
5. **章节创作** — 输入章节号+标题，AI 根据全局上下文自动写作
6. **AI创作结果** — 审阅、润色、修订生成内容，支持选中重写、按指令修改
7. **完稿审校** — 检查伏笔状态、因果链、情感弧线、前情摘要

#### 模式二：参考小说创作
上传参考小说 TXT，AI 分析后辅助创作：

1. **分析阶段** — 上传 TXT 文件，AI 分块分析提取：书名、题材、核心主题、世界观设定、角色原型、叙事结构、语言风格
2. **选择创作方式**:
   - **使用分析结果创作** — 保留参考小说的故事框架、角色和世界观
   - **修改后创作** — 修改角色名、调整设定，借鉴风格创作全新故事
3. **写作工作台** — 左侧参考面板（概览/角色/风格三Tab）+ 右侧写作区

### 4.2 五层 Agent 写作管线

章节写作采用完整的五层 Agent 架构：

```
建筑师规划 → 写手创作 → 验证器检查 → 审计器审查 → 因果提取
                ↑                                    |
                └──────── 修订闭环（最多2轮）←────────┘
```

1. **ArchitectAgent (建筑师)** — 写作前调用 AI 规划章节蓝图
   - 场景结构：开场→发展→高潮→收尾
   - 关键节拍：事件+情感目标
   - 伏笔策略：推进/埋设/回收
   - 因果衔接：与前章因果连接点
   - 角色调度：出场角色及其作用
   - 情感弧线：起始→转折→终点
   - 字数分配：各场景字数建议

2. **Writer (写手)** — 将建筑师蓝图注入 prompt，AI 执行写作

3. **PostWriteValidator (验证器)** — 写后质量检查
   - 通用验证：AI标记词密度、禁止句式、元叙事检测、报告式语言、集体反应套话、连续"了"字、长段落、字数偏差
   - MiMo 专项验证：形容词密度、描写占比、连续长句

4. **Auditor (审计器)** — 叙事一致性审计
   - 因果一致性 (causal_consistency)
   - 角色OOC检测 (character_ooc)
   - 钩子缺失 (hook_missing)
   - 节奏问题 (pacing)
   - 信息边界 (info_boundary)

5. **Causal Extractor (因果提取)** — 从章节中提取因果链、伏笔、情感变化

**修订闭环**: 审计发现 critical 问题时，自动触发修订（最多2轮），修订后重新验证。

### 4.3 写后结算（8项）

每章写作完成后自动执行：

1. 摘要生成 — 更新前情摘要
2. 因果链结算 — 提取并存储因果链
3. 伏笔结算 — 推进/回收伏笔状态
4. 情感弧线结算 — 记录角色情感快照
5. 关系网络结算 — 更新角色关系强度
6. 信息边界结算 — 记录角色所知信息
7. 戏剧节拍结算 — 标记章节节拍类型
8. 叙事线程结算 — 更新多线叙事状态

### 4.4 写作编辑器

- **撤销/重做栈** — 支持50步操作历史
- **30秒自动保存** — 停止输入后自动触发
- **AI 写作助手** — 内置 BottomSheet 对话
  - 快捷操作：续写、润色、建议、扩写、大纲、对话
  - 支持将 AI 回复插入正文
- **章节切换** — 下拉菜单选择章节，自动保存当前章节

### 4.5 写作技能蒸馏（风格迁移）

上传参考小说 TXT，AI 蒸馏其写作风格，生成 5 个维度的风格指纹：

- 句式模式：长短句、断句、标点、段落
- 词汇指纹：高频词、禁用词、惯用搭配
- 叙事手法：视角、白描/渲染、对话占比
- 对话风格：语气词、称呼、交锋模式
- 节奏特征：段落分布、场景切换、张弛

蒸馏后的风格注入 AI 写作 prompt，使生成内容模仿目标风格。

### 4.6 内容整改管线

`ContentRectifier` 在 AI 写作后对内容进行三级整改：

1. **第一优先级：逻辑与一致** — 前后矛盾检查、角色名一致性、长句拆分
2. **第二优先级：去AI味** — 删除AI套话（仿佛、忽然、竟然等）、报告式语言、流水账、概括性总结
3. **第三优先级：拟人化增强** — 句子节奏改造、对话"废话"注入、心理描写动作化、感官锚点

支持口语化创作和引用梗写作两种可选风格。

### 4.7 上下文管理

4层上下文确保章节连贯性：

| 层级 | 内容 | 容量 |
|------|------|------|
| 前情摘要 | 已完成章节摘要 | ≤8000字（智能分配） |
| 上一章结尾 | 最近章节结尾 | 500字 |
| 因果链 | 事件因果关系 | 2000字 |
| 待回收伏笔 | 未闭合伏笔 | 1000字 |

**长篇小说智能策略**: 摘要 >8000字时，取前2000字（总览）+ 后6000字（近期），中间用省略提示；摘要 >15000字时触发 AI 压缩。

### 4.8 侧边栏导航

```
┌─ 创作 ─────────────────┐
│  🏠 书架               │
│  📝 创作流程/参考创作   │  ← 根据书籍类型动态切换
│  👥 角色档案           │
│  ✍️ 写作               │
│  🎨 写作技能           │
├─ 分析 ─────────────────┤
│  📊 一致性追踪         │
│  📅 时间线             │
├─ 工具 ─────────────────┤
│  📄 TXT拆分            │
│  ⚙️ 设置               │
└────────────────────────┘
```

### 4.9 书架管理

- **创建新作品** — 三选一对话框：全新创作 / 参考小说创作
- **重命名** — 点击书籍右侧"重命名"按钮
- **长按显示完整书名** — 书名过长时自动省略，长按弹窗显示完整标题
- **导出** — 复制到剪贴板（短文本）或通过文件分享（>500KB）
- **删除** — 确认弹窗，删除书籍、章节、写作技能

### 4.10 多书并行

- 每本书独立的 `BookSession`，互不干扰
- 顶部 Tab 栏快速切换书籍（≥2本时显示）
- Session 快照/恢复机制，切换时保存/恢复完整状态

### 4.11 API 设置

- 兼容 OpenAI 格式 API（DeepSeek 等）
- 可配置：API 地址、API Key、模型名称、Temperature、Max Tokens、System Prompt
- **测试连接**按钮验证 API 可用性
- URL 自动补全 `https://` 前缀

---

## 五、UI 设计系统

### 5.1 配色方案（暖色纸质风格）

| 用途 | 颜色值 | 变量名 |
|------|--------|--------|
| 主背景 | `#FAF8F5` | `BgPrimary` |
| 卡片背景 | `#FFFFFF` | `BgCard` |
| 侧边栏 | `#F0EBE3` | `BgSidebar` |
| 主文字 | `#2C2418` | `TextPrimary` |
| 次文字 | `#6B5D4F` | `TextSecondary` |
| 辅助文字 | `#9C8E7E` | `TextTertiary` |
| 强调色 | `#C47D3B` | `Accent` (琥珀/铜色) |
| 成功 | `#5A8C5A` | `Success` |
| 危险 | `#B85450` | `Danger` |
| 信息 | `#5A7A8C` | `Info` |

### 5.2 角色类型色

| 角色 | 颜色 |
|------|------|
| 主角 | Accent (琥珀) |
| 反派 | Danger (红色) |
| 影响者 | Info (蓝色) |
| 守护者 | Success (绿色) |
| 伙伴 | 紫色 |
| 阻碍者 | 金色 |
| 怀疑者 | 青灰 |

### 5.3 通用组件库 (`Components.kt`)

- `InkCard` — 圆角卡片（带阴影+边框）
- `InkButton` — 主按钮/轮廓按钮
- `CharacterAvatar` — 圆形角色头像
- `InkTag` — 标签
- `StatChip` — 统计芯片（首页横向滚动）
- `EmptyState` — 空状态占位
- `TopbarButton` — 工具栏按钮
- `InkProgressBar` — 进度条
- `InkDivider` / `InkDividerLight` — 分割线
- `SectionHeader` — 区域标题
- `InkToast` — Toast 提示
- `BookBanner` — 书籍信息渐变横幅
- `StepProgressBar` — 7步进度条
- `InkSwitch` — 自定义开关
- `StatRow` — 统计数字行

---

## 六、关键设计决策

### 6.1 BookSession 模式
每本书拥有独立的 `BookSession`，持有该书的完整创作状态（步骤、配置、大纲、因果链、摘要等）。切换书籍时通过快照保存/恢复，支持多书并行写作。

### 6.2 状态派生
所有创作状态通过 `sessionState()` 从活跃会话动态派生，保证状态始终与当前书籍一致。

### 6.3 参考创作状态同步
在 `selectBook` 中同步检查 session 中的 `referenceAnalysis`，立即设置 `currentBookIsReference`，避免 UI 闪现到错误的创作界面。

### 6.4 修订闭环
写作后发现 critical 问题时自动触发修订（最多2轮），修订后重新验证，确保输出质量。

### 6.5 智能上下文
长篇小说场景下，上下文自动分配：短篇全取，中篇分段取，长篇触发 AI 压缩。

---

## 七、数据流

```
用户操作 → MainViewModel → LocalRepository → Room DB
                ↓
           AiRepository → HTTP API → AI 模型
                ↓
           ArchitectAgent → 蓝图注入 prompt
                ↓
           ContentRectifier → 整改润色
                ↓
           PostWriteValidator → 质量验证
                ↓
           BookSession → 状态更新 → UI 刷新
```

---

## 八、构建配置

- **编译 SDK**: 35
- **最低 SDK**: 26
- **Java 版本**: 17
- **Compose BOM**: 2025.05.00
- **Room**: 2.6.1 (KSP 编译)
- **序列化**: kotlinx-serialization-json 1.7.3
- **发布版**: 启用混淆 (ProGuard)

---

> 本文档自动生成于 2026-07-27，基于项目源代码分析。